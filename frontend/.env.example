# Backend API base URL used by the Vite dev server.
# Vite inlines VITE_* variables into the JS bundle at build time — they are
# public. Never put secrets in VITE_* variables.
VITE_API_URL=http://localhost:8000
